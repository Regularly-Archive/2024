{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    { "datasource": { "type": "prometheus", "uid": "prometheus" }, "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] }, "gridPos": { "h": 10, "w": 12, "x": 0, "y": 0 }, "id": 1, "targets": [{ "expr": "histogram_quantile(0.50, sum by (le, gen_ai_request_model) (rate(insighta_agent_round_duration_milliseconds_bucket[$__rate_interval])))", "legendFormat": "{{gen_ai_request_model}} p50" }, { "expr": "histogram_quantile(0.95, sum by (le, gen_ai_request_model) (rate(insighta_agent_round_duration_milliseconds_bucket[$__rate_interval])))", "legendFormat": "{{gen_ai_request_model}} p95" }], "title": "Agent round latency by model", "type": "timeseries" },
    { "datasource": { "type": "prometheus", "uid": "prometheus" }, "fieldConfig": { "defaults": { "unit": "short", "decimals": 2 }, "overrides": [] }, "gridPos": { "h": 10, "w": 12, "x": 12, "y": 0 }, "id": 2, "targets": [{ "expr": "(sum(rate(insighta_agent_round_rounds_total[$__rate_interval])) / sum(rate(insighta_agent_run_runs_total[$__rate_interval]))) and on() (sum(rate(insighta_agent_run_runs_total[$__rate_interval])) > 0)", "legendFormat": "rounds per turn" }], "title": "Average rounds per turn", "type": "timeseries" }
  ],
  "schemaVersion": 41,
  "tags": ["insighta", "opentelemetry", "agent"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "InsightaAI Agent",
  "uid": "insighta-agent",
  "version": 1
}
